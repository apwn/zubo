FROM oven/bun:1

WORKDIR /app

COPY package.json bun.lock* ./
RUN bun install --production

COPY . .

VOLUME /data

ENV REGISTRY_DB_PATH=/data/registry.db
ENV REGISTRY_PORT=3000

EXPOSE 3000

CMD ["bun", "run", "src/registry/server.ts"]
